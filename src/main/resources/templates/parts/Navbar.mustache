
<nav class="navbar navbar-expand-lg navbar-dark bg-dark" >
    <a class="navbar-brand" href="/">Моя косметичка</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item" >
                <a class="nav-link" href="/" > <i class="fas fa-home"></i> Домой <span class="sr-only"></span></a>
            </li>
            <li class="nav-item" >
                <a class="nav-link" href="/app"  ><i class="fas fa-shopping-bag"></i> Моя косметичка <span class="sr-only"></span></a>
            </li>
            {{#user.isAdmin}}
            <li class="nav-item">
                <a class="nav-link" href="/user"  ><i class="fas fa-user-edit"></i> Редактировать пользователей <span class="sr-only"></span></a>
            </li>
            {{/user.isAdmin}}
        </ul>
        <ul class="navbar-nav mr-right" >
            <li>
                <a class="nav-link" href="/profile/{{user.getId}}" ><i class="fas fa-user"></i> {{user.getUsername}}</a>
            </li>
        </ul>

        <form action="/logout" method="post">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <button class="btn btn-primary" type="submit"><i class="fa fa-sign-out-alt"></i> Выйти</button>
        </form>
    </div>
</nav>

