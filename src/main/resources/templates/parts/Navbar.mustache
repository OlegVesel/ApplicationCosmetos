<nav class="navbar navbar-expand-lg navbar-dark bg-dark" >
    <a class="navbar-brand" href="/">Косметос</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="/">Домой <span class="sr-only"></span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/app">Моя косметика <span class="sr-only"></span></a>
            </li>
            {{#user.isAdmin}}
            <li class="nav-item">
                <a class="nav-link" href="/user">Редактировать пользователей <span class="sr-only"></span></a>
            </li>
            {{/user.isAdmin}}
        </ul>
        <div class="navbar-text mr-3">{{user.getUsername}}</div>

        <form action="/logout" method="post">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <button class="btn btn-primary" type="submit"><i class="fa fa-sign-out-alt"></i> Выйти</button>
        </form>

    </div>
</nav>